# Corgi corpus

Scraped once with Firecrawl. The public agent reads these files.
It does not call Firecrawl at run time.

- Origin: https://www.corgi.insure
- Pages: 43
- Saved: 2026-09-08T02:24:02.439Z

Blog paths are omitted.

The quote app product page (`pages/quote__products.md`) is a logged-in walk of app.corgi.insure. It is not a Firecrawl scrape.

Lookup: `site_lookup` scores `pages/*.md` and returns snippets.