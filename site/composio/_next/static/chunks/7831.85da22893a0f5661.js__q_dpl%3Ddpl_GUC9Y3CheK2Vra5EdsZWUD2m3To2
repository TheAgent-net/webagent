"use strict";(self.webpackChunk_N_E=self.webpackChunk_N_E||[]).push([[7831],{17285:(e,t,n)=>{n.d(t,{s:()=>l});var a=n(97467),r=n(1410);function l(){let{camera:e}=(0,a.C)(),t=(0,r.useRef)({x:0,y:0});return(0,a.D)(n=>{let a=n.pointer.x,r=n.pointer.y;t.current.x+=(.015*r-t.current.x)*.03,t.current.y+=(-(.02*a)-t.current.y)*.03,e.rotation.x=t.current.x,e.rotation.y=t.current.y,e.fov=75,e.updateProjectionMatrix()}),null}},57831:(e,t,n)=>{n.r(t),n.d(t,{WarpTunnelCanvas:()=>k});var a=n(35466),r=n(87225),l=n(62565),o=n(97467),i=n(1410),s=n(47191),c=n(43324),u=n(90779),f=n(95748);let d=[[.7,.75,.85],[.5,.55,.95],[.3,.8,.9],[.6,.6,.7],[.4,.45,1]];function v(e){let{streakCount:t=120}=e,n=(0,i.useRef)(null),r=(0,i.useRef)(!1),l=(0,i.useRef)(0),{offsets:v,scales:h,colors:p,speeds:m,opacities:g}=(0,i.useMemo)(()=>(function(e){let t=new Float32Array(3*e),n=new Float32Array(2*e),a=new Float32Array(3*e),r=new Float32Array(e),l=new Float32Array(e);for(let i=0;i<e;i++){var o;let s=i<e/2?-1:1;t[3*i+0]=20*s,t[3*i+1]=(Math.random()-.5)*36,t[3*i+2]=100*Math.random(),n[2*i+0]=8+24*Math.random(),n[2*i+1]=.08+.15*Math.random();let c=Math.floor(Math.random()*d.length),u=null!=(o=d[c])?o:d[0];a[3*i+0]=u[0],a[3*i+1]=u[1],a[3*i+2]=u[2],r[i]=1.8+1.2*Math.random(),l[i]=.15+.25*Math.random()}return{offsets:t,scales:n,colors:a,speeds:r,opacities:l}})(t),[t]),b=(0,i.useMemo)(()=>({uTime:{value:0},uBaseSpeed:{value:1},uShrink:{value:0}}),[]);(0,i.useEffect)(()=>{r.current=window.matchMedia("(prefers-reduced-motion: reduce)").matches},[]);let x=(0,i.useRef)(0);return(0,o.D)((e,t)=>{let n;if(r.current)return;let a=e.clock.elapsedTime,o=Math.min(t,.1);if(a<4){let e=a/4;n=22+-16*(1-(1-e)*(1-e)*(1-e))}else n=6;let i=+!!c.Q.boost;x.current+=(i-x.current)*.08,n+=30*x.current,l.current+=o*n,b.uTime.value=l.current,b.uShrink.value=.5*x.current}),(0,a.jsxs)("instancedMesh",{args:[void 0,void 0,t],frustumCulled:!1,ref:n,children:[(0,a.jsxs)("planeGeometry",{args:[1,1],children:[(0,a.jsx)("instancedBufferAttribute",{args:[v,3],attach:"attributes-aOffset"}),(0,a.jsx)("instancedBufferAttribute",{args:[h,2],attach:"attributes-aScale"}),(0,a.jsx)("instancedBufferAttribute",{args:[p,3],attach:"attributes-aColor"}),(0,a.jsx)("instancedBufferAttribute",{args:[m,1],attach:"attributes-aSpeed"}),(0,a.jsx)("instancedBufferAttribute",{args:[g,1],attach:"attributes-aOpacity"})]}),(0,a.jsx)("shaderMaterial",{blending:s.EZo,depthWrite:!1,fragmentShader:u.S,side:s.$EB,transparent:!0,uniforms:b,vertexShader:f.U})]})}function h(e){let{streakCount:t=80}=e,n=(0,i.useRef)(null),r=(0,i.useRef)(0);return(0,o.D)(()=>{let e=+!!c.Q.boost;if(r.current+=(e-r.current)*.04,n.current){let e=r.current>.01;n.current.visible=e,e&&n.current.traverse(e=>{e instanceof s.eaF&&e.material&&(e.material.opacity=r.current)})}}),(0,a.jsx)("group",{ref:n,visible:!1,children:(0,a.jsx)(v,{streakCount:t})})}var p=n(99292),m=n(51257),g=n(12732),b=n(4957);let x=new s.Q1f(1710618),y={uniforms:{tDiffuse:{value:null},uResolution:{value:new s.I9Y(1,1)},uPixelSize:{value:6},uTime:{value:0}},vertexShader:"\n		varying vec2 vUv;\n		void main() {\n			vUv = uv;\n			gl_Position = projectionMatrix * modelViewMatrix * vec4(position, 1.0);\n		}\n	",fragmentShader:"\n		uniform sampler2D tDiffuse;\n		uniform vec2 uResolution;\n		uniform float uPixelSize;\n		uniform float uTime;\n		varying vec2 vUv;\n\n		// Hash for per-pixel noise\n		float hash(vec2 p) {\n			return fract(sin(dot(p, vec2(127.1, 311.7))) * 43758.5453);\n		}\n\n		void main() {\n			vec2 pixelCoord = vUv * uResolution;\n			vec2 cell = floor(pixelCoord / uPixelSize);\n			vec2 cellCenter = (cell + 0.5) * uPixelSize;\n			vec2 cellUv = cellCenter / uResolution;\n\n			// Chromatic aberration — slight RGB offset sampling\n			float caOffset = 0.8 / uResolution.x;\n			float r = texture2D(tDiffuse, cellUv + vec2(caOffset, 0.0)).r;\n			float g = texture2D(tDiffuse, cellUv).g;\n			float b = texture2D(tDiffuse, cellUv - vec2(caOffset, 0.0)).b;\n			vec4 color = vec4(r, g, b, 1.0);\n\n			// Local position within cell (0-1)\n			vec2 localPos = fract(pixelCoord / uPixelSize);\n\n			// Each LED pixel is split into 3 RGB sub-pixels side by side\n			float subPixelWidth = 1.0 / 3.0;\n			float subPixelX = localPos.x / subPixelWidth;\n			int subPixelIdx = int(floor(subPixelX));\n			float subLocalX = fract(subPixelX);\n\n			// Sub-pixel gap (dark line between sub-pixels)\n			float subGapX = smoothstep(0.0, 0.08, subLocalX) * smoothstep(1.0, 0.92, subLocalX);\n			// Vertical gap at top and bottom of cell\n			float subGapY = smoothstep(0.0, 0.06, localPos.y) * smoothstep(1.0, 0.94, localPos.y);\n			float subGap = subGapX * subGapY;\n\n			// Color per sub-pixel channel\n			vec3 subColor = vec3(0.0);\n			float brightness = max(color.r, max(color.g, color.b));\n\n			if (subPixelIdx == 0) {\n				subColor = vec3(color.r, color.r * 0.05, color.r * 0.02);\n			} else if (subPixelIdx == 1) {\n				subColor = vec3(color.g * 0.02, color.g, color.g * 0.05);\n			} else {\n				subColor = vec3(color.b * 0.05, color.b * 0.02, color.b);\n			}\n\n			// Blend: mostly show combined color, but let sub-pixels peek through on bright areas\n			float subPixelStrength = 0.3 + brightness * 0.4;\n			vec3 combined = mix(color.rgb, subColor, subPixelStrength);\n\n			// Per-pixel manufacturing variance — subtle brightness jitter\n			float pixelNoise = 0.92 + hash(cell) * 0.08;\n\n			// Square LED shape with slight rounded edges\n			vec2 edge = abs(localPos - 0.5);\n			float maxEdge = max(edge.x, edge.y);\n			float dot = smoothstep(0.48, 0.44, maxEdge);\n\n			// Scanning refresh line — slow sweep down the screen\n			float scanY = fract(uTime * 0.12);\n			float scanDist = abs(vUv.y - scanY);\n			float scanLine = 1.0 + smoothstep(0.06, 0.0, scanDist) * 0.7;\n\n			vec3 result = combined * subGap * dot * pixelNoise * scanLine;\n\n			// Glow bleed — bright pixels bleed into neighbors\n			result += color.rgb * brightness * 0.08;\n\n			gl_FragColor = vec4(result, 1.0);\n		}\n	"};function S(e){let{isMobile:t=!1}=e,{gl:n,scene:a,camera:r,size:l}=(0,o.C)(),{composer:c,ledPass:u}=(0,i.useMemo)(()=>{n.setClearColor(1710618,1),a.background=x;let e=new p.s(n),t=new m.A(a,r);t.clearColor=x,t.clearAlpha=1,e.addPass(t),e.addPass(new b.C(new s.I9Y(1,1),.15,.8,.1));let l=new g.p(y);return e.addPass(l),{composer:e,ledPass:l}},[n,a,r]);return(0,i.useEffect)(()=>{let e=u.uniforms.uPixelSize;e&&(e.value=t?4:6)},[t,u]),(0,i.useEffect)(()=>{var e;c.setSize(l.width,l.height),null==(e=u.uniforms.uResolution)||e.value.set(l.width,l.height)},[c,u,l.width,l.height]),(0,i.useEffect)(()=>()=>c.dispose(),[c]),(0,o.D)(()=>{let e=u.uniforms.uTime;e&&(e.value=performance.now()/1e3),c.render()},1),null}var w=n(17285),M=n(98862),C=n(81257);let O=[[.12,.21,1],[.12,.22,.92],[.15,.22,.95],[.08,.3,.26],[.25,.05,.85],[.18,.25,.43],[0,1,1],[0,.537,1]],D=O.map((e,t)=>t),F=[0,1,2,4,5,7];function P(e){let{instanceCount:t=200,isMobile:n=!1}=e,r=(0,i.useRef)(null),l=(0,i.useRef)(!1),u=(0,i.useRef)(0),{offsets:f,scales:d,colors:v,speeds:h,colorModes:p,opacities:m,grains:g,edgeGlows:b}=(0,i.useMemo)(()=>(function(e){let t=arguments.length>1&&void 0!==arguments[1]&&arguments[1],n=new Float32Array(3*e),a=new Float32Array(2*e),r=new Float32Array(3*e),l=new Float32Array(e),o=new Float32Array(e),i=new Float32Array(e),s=new Float32Array(e),c=new Float32Array(e),u=t?40:108,f=t?80:160;for(let m=0;m<e;m++){var d,v,h,p;let g=m<e/2?-1:1,b=u+Math.random()*f;if(t){let e=20+b/(u+f)*100;n[3*m+0]=(Math.random()-.5)*e,n[3*m+1]=g*b}else{n[3*m+0]=g*b;let e=30+b/(u+f)*200;n[3*m+1]=(Math.random()-.5)*e}n[3*m+2]=100*Math.random(),a[2*m+0]=t?15+30*Math.random():25+50*Math.random(),a[2*m+1]=t?5+10*Math.random():8+16*Math.random(),l[m]=1.2+.8*Math.random(),o[m]=+(.3>Math.random()),i[m]=t?.3>Math.random()?.95:.55+.3*Math.random():.3>Math.random()?.85:.4+.25*Math.random(),s[m]=+(.6>Math.random()),c[m]=+(.3>Math.random());let x=(null!=(d=c[m])?d:0)>.5?F:D,y=null!=(p=null!=(h=O[null!=(v=x[Math.floor(Math.random()*x.length)])?v:0])?h:O[0])?p:[.9,.9,.95];r[3*m+0]=y[0],r[3*m+1]=y[1],r[3*m+2]=y[2]}return{offsets:n,scales:a,colors:r,speeds:l,colorModes:o,opacities:i,grains:s,edgeGlows:c}})(t,n),[t,n]),x=(0,i.useMemo)(()=>({uTime:{value:0},uBaseSpeed:{value:1},uShrink:{value:0},uVertical:{value:+!!n}}),[n]);(0,i.useEffect)(()=>{l.current=window.matchMedia("(prefers-reduced-motion: reduce)").matches},[]);let y=(0,i.useRef)(0);return(0,o.D)((e,t)=>{let n;if(l.current)return;let a=e.clock.elapsedTime,r=Math.min(t,.1);if(a<4){let e=a/4;n=30+-18*(1-(1-e)*(1-e)*(1-e))}else n=12;let o=+!!c.Q.boost;y.current+=(o-y.current)*.08,n+=40*y.current,u.current+=r*n,x.uTime.value=u.current,x.uShrink.value=.5*y.current}),(0,a.jsxs)("instancedMesh",{args:[void 0,void 0,t],frustumCulled:!1,ref:r,children:[(0,a.jsxs)("planeGeometry",{args:[1,1],children:[(0,a.jsx)("instancedBufferAttribute",{args:[f,3],attach:"attributes-aOffset"}),(0,a.jsx)("instancedBufferAttribute",{args:[d,2],attach:"attributes-aScale"}),(0,a.jsx)("instancedBufferAttribute",{args:[v,3],attach:"attributes-aColor"}),(0,a.jsx)("instancedBufferAttribute",{args:[h,1],attach:"attributes-aSpeed"}),(0,a.jsx)("instancedBufferAttribute",{args:[p,1],attach:"attributes-aColorMode"}),(0,a.jsx)("instancedBufferAttribute",{args:[m,1],attach:"attributes-aOpacity"}),(0,a.jsx)("instancedBufferAttribute",{args:[g,1],attach:"attributes-aGrain"}),(0,a.jsx)("instancedBufferAttribute",{args:[b,1],attach:"attributes-aEdgeGlow"})]}),(0,a.jsx)("shaderMaterial",{blending:s.NTi,depthWrite:!1,fragmentShader:M.S,side:s.$EB,transparent:!0,uniforms:x,vertexShader:C.U})]})}function k(){let e=(0,l.a)();return(0,a.jsxs)(r.Hl,{camera:{position:[0,.5,0],fov:75,near:.1,far:100},dpr:1,gl:{antialias:!1,powerPreference:"high-performance",alpha:!0},style:{background:"transparent"},children:[(0,a.jsx)(w.s,{}),(0,a.jsx)(P,{instanceCount:e?60:120,isMobile:e}),!e&&(0,a.jsx)(h,{streakCount:50}),(0,a.jsx)(S,{isMobile:e})]})}},81257:(e,t,n)=>{n.d(t,{U:()=>a});let a="\nattribute vec3 aOffset;\nattribute vec2 aScale;\nattribute vec3 aColor;\nattribute float aSpeed;\nattribute float aColorMode;\nattribute float aOpacity;\nattribute float aGrain;\nattribute float aEdgeGlow;\nuniform float uTime;\nuniform float uBaseSpeed;\nuniform float uShrink;\nuniform float uVertical;\n\nvarying vec2 vUv;\nvarying vec3 vColor;\nvarying float vColorMode;\nvarying float vFade;\nvarying float vOpacity;\nvarying float vGrain;\nvarying float vEdgeGlow;\n\nvoid main() {\n  vUv = uv;\n  vColor = aColor;\n  vGrain = aGrain;\n  vEdgeGlow = aEdgeGlow;\n  vColorMode = aColorMode;\n\n  // Scale the unit quad (position.xy goes -0.5 to 0.5)\n  vec2 scaled = position.xy * aScale;\n\n  // Animate z: mod() for seamless infinite recycling\n  float totalDepth = 100.0;\n  float zOffset = mod(aOffset.z - uTime * uBaseSpeed * aSpeed, totalDepth);\n  float centerZ = -(zOffset - 6.0);\n  float z = centerZ + scaled.x;\n\n  // S-curve pinch — gradual at both ends, steeper in the middle\n  float centerDepth = max(0.0, -centerZ);\n  float t = smoothstep(0.0, 94.0, centerDepth);\n  float pinch = mix(1.0, 0.4, t);\n\n  float shrinkScale = 1.0 - uShrink * 0.3;\n  float minGap = 18.0 * shrinkScale;\n\n  vec3 center;\n  vec3 local;\n\n  if (uVertical > 0.5) {\n    // Mobile: top/bottom walls\n    center.x = aOffset.x * pinch * shrinkScale;\n    center.y = aOffset.y * pinch * shrinkScale + sign(aOffset.y) * minGap;\n    center.z = centerZ;\n    local.x = scaled.y * pinch;\n    local.y = 0.0;\n    local.z = scaled.x;\n  } else {\n    // Desktop: left/right walls\n    center.x = aOffset.x * pinch * shrinkScale + sign(aOffset.x) * minGap;\n    center.y = aOffset.y * pinch * shrinkScale;\n    center.z = centerZ;\n    local.x = 0.0;\n    local.y = scaled.y * pinch;\n    local.z = scaled.x;\n  }\n\n  vec3 wallPos = center + local;\n\n  // Sharp fade only at the very far end to prevent pop-in\n  float normalizedDepth = zOffset / totalDepth;\n  vFade = smoothstep(1.0, 0.7, normalizedDepth);\n  vOpacity = aOpacity;\n\n  gl_Position = projectionMatrix * modelViewMatrix * vec4(wallPos, 1.0);\n}\n"},90779:(e,t,n)=>{n.d(t,{S:()=>a});let a="\nvarying vec2 vUv;\nvarying vec3 vColor;\nvarying float vFade;\nvarying float vOpacity;\n\nvoid main() {\n  // Fade along the length (x axis) — bright in center, transparent at ends\n  float lengthFade = 1.0 - 2.0 * abs(vUv.x - 0.5);\n  lengthFade = lengthFade * lengthFade;\n\n  // Soft edges along height\n  float edgeSoft = 1.0 - 2.0 * abs(vUv.y - 0.5);\n  edgeSoft = smoothstep(0.0, 0.4, edgeSoft);\n\n  float alpha = vOpacity * vFade * lengthFade * edgeSoft;\n  gl_FragColor = vec4(vColor, alpha);\n}\n"},95748:(e,t,n)=>{n.d(t,{U:()=>a});let a="\nattribute vec3 aOffset;\nattribute vec2 aScale;\nattribute vec3 aColor;\nattribute float aSpeed;\nattribute float aOpacity;\nuniform float uTime;\nuniform float uBaseSpeed;\nuniform float uShrink;\n\nvarying vec2 vUv;\nvarying vec3 vColor;\nvarying float vFade;\nvarying float vOpacity;\n\nvoid main() {\n  vUv = uv;\n  vColor = aColor;\n  vOpacity = aOpacity;\n\n  vec2 scaled = position.xy * aScale;\n\n  float totalDepth = 100.0;\n  float zOffset = mod(aOffset.z - uTime * uBaseSpeed * aSpeed, totalDepth);\n  float centerZ = -(zOffset - 6.0);\n\n  float centerDepth = max(0.0, -centerZ);\n  float t = smoothstep(0.0, 94.0, centerDepth);\n  float pinch = mix(1.0, 0.4, t);\n\n  float shrinkScale = 1.0 - uShrink * 0.3;\n  float minGap = 18.0 * shrinkScale;\n\n  vec3 center;\n  center.x = aOffset.x * pinch * shrinkScale + sign(aOffset.x) * minGap;\n  center.y = aOffset.y * pinch * shrinkScale;\n  center.z = centerZ;\n\n  vec3 local;\n  local.x = 0.0;\n  local.y = scaled.y * pinch;\n  local.z = scaled.x;\n\n  vec3 wallPos = center + local;\n\n  float normalizedDepth = zOffset / totalDepth;\n  vFade = smoothstep(1.0, 0.7, normalizedDepth);\n\n  gl_Position = projectionMatrix * modelViewMatrix * vec4(wallPos, 1.0);\n}\n"},98862:(e,t,n)=>{n.d(t,{S:()=>a});let a="\nuniform float uTime;\n\nvarying vec2 vUv;\nvarying vec3 vColor;\nvarying float vColorMode;\nvarying float vFade;\nvarying float vOpacity;\nvarying float vGrain;\nvarying float vEdgeGlow;\n\nfloat random(vec2 st) {\n  return fract(sin(dot(st, vec2(12.9898, 78.233))) * 43758.5453);\n}\n\nvoid main() {\n  vec2 uv = vUv;\n\n  // Distance from edge of the quad (0 at edge, 0.5 at center)\n  float dx = min(uv.x, 1.0 - uv.x);\n  float dy = min(uv.y, 1.0 - uv.y);\n  float edgeDist = min(dx, dy);\n\n  // Fill color\n  vec3 fill = vColor;\n\n  // Gradient mode: linear sweep from darker to brighter (branchless)\n  vec3 gradFill = mix(fill * 0.5, fill * 1.5, uv.x);\n  fill = mix(fill, gradFill, step(0.5, vColorMode));\n\n  // Film grain — UV-based so it stays stable per-instance (no flicker)\n  float grain = random(uv * 64.0 + floor(uTime * 0.5));\n  fill += (grain - 0.5) * 0.08 * step(0.5, vGrain);\n\n  // Edge glow: bright at borders, cubic falloff toward center (branchless)\n  float t = clamp(edgeDist / 0.5, 0.0, 1.0);\n  float glow = (1.0 - t) * (1.0 - t) * (1.0 - t);\n  fill = mix(fill, fill * 1.8, glow * step(0.5, vEdgeGlow));\n\n  gl_FragColor = vec4(fill, vOpacity * vFade);\n}\n"}}]);